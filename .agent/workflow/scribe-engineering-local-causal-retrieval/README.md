# Compatibility Shim

This path is deprecated. The portable SCRIBE workflow bundle now lives at:

```text
.agent/workflow/scribe/
```

Use `.agent/workflow/scribe/scribe` for SEL maintenance operations. This folder
only preserves the historical command path for older agents and scripts.
